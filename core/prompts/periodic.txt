This session is still open — this is an idle mid-session checkpoint, not a close-out. Run a lightweight pass of the same self-improvement pipeline against what has happened so far, without asking for confirmation. Skip anything that needs the full session (LEVEL scoring is close-out only — see session-end.txt).

Prerequisite skills (optional, same as the close-out pass): `task-observer` (SCAN), `skill-creator`/`skill-compactor` (SKILL), `learn-from-session` (GUARDRAILS), `memorix`/`claude-mem` (MEMORY — primary store when reachable). A `DETECTED (computed by the hook...)` line near the end of this prompt tells you exactly which of the first four are installed — go by that, not a guess (memorix/claude-mem reachability isn't in it; check that one directly, since it's this-session-specific). Prefer any tool the DETECTED line marks `=yes` over the fallback below.

1. SCAN. Reread what has happened in this session so far (it is not finished — more may follow after this checkpoint). If a "DELTA FRAME" line appears below, a prior checkpoint already reflected on everything up to that time — scan ONLY newer activity and do not re-store what a previous pass already captured (dedupe against MEMORY.md). If nothing new has happened since, say "nothing new since last checkpoint" and stop.
2. IDENTIFY. Classify anything found exactly as in session-end.txt step 2: MEMORY / GUARDRAILS / SKILL. If nothing qualifies yet, that's expected — say so and stop, don't pad the report.
3. MEMORY + PROJECT-RULE. Decide the LEVEL (global vs this-repo) — see `core/prompts/reference/project-rules.txt`. Global insight: store in `memorix`/`claude-mem` as primary when reachable, and ALWAYS also append to `~/.agent-flywheel/MEMORY.md` (baseline). A durable convention specific to THIS repo goes in the project's own AGENTS.md/CLAUDE.md instead of only your personal store.
4. GUARDRAILS. A user correction this session that must bind immediately cannot wait for close-out: per the DETECTED line, use `learn-from-session` if `=yes`, otherwise follow `core/prompts/reference/guardrails-format.txt` — now, same as the close-out pass. This is exactly the case where "binding starting the next opportunity to apply it" means later in THIS same session.
5. SKILL. If a workflow has already repeated 3+ times so far and nothing covers it, run the CHECK-EXISTING → UPDATE/CREATE → COMPACT sequence from `core/prompts/reference/skill-roots.txt` + `skill-authoring.txt` now rather than waiting for close-out — a mid-session skill fix means the rest of THIS session benefits from it too.
6. META (only if the user opted the meta pass into mid-session). Run `<CLI> self-improve --gate --trigger mid-session --mark`, where `<CLI>` is the agent-flywheel CLI path given at the very end of this prompt (do not assume it is on PATH). Exit 0 → it prints the meta prompt; follow it now against the flywheel checkout + your `~/.agent-flywheel/*` files. Exit non-zero → not due at this trigger (the default cadence runs the meta pass at session-end, not mid-session); skip silently.
7. Do NOT treat this as a session end: do not call any "session end"/"wrap up"/finalize tool or mechanism this harness exposes — the session is still running and will continue after this checkpoint.

Append one line to `~/.agent-flywheel/LEARN.log` (same format as the close-out pass) if anything changed, then give a one-line summary of what was captured (or "nothing new yet") and stop.
