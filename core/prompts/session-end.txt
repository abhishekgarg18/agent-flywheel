This session just ended. Run the self-improvement reflection pass now, without asking for confirmation.

Ground yourself first: the session transcript is referenced below — read it directly (whatever read/file tool this harness exposes). This reflection pass's own conversation history starts blank; it is NOT pre-loaded with that transcript just because it resumed the session.

Follow this procedure (episodic scan -> reflect -> semantic write -> proceduralize -> decay stale entries — the standard reflection-agent pattern, not free-form summarizing):

1. SCAN (episodic). Reread what happened this session: mistakes made and corrected, non-obvious facts discovered about this codebase/environment, explicit user preferences or corrections stated, repeatable procedures used more than once, and anything a future session would otherwise have to rediscover from scratch.
2. REFLECT (extract, don't transcribe). Pick at most 2-3 items that are genuinely reusable — a DIFFERENT future session hitting a similar situation would benefit from knowing this. Reject anything that's just a play-by-play of this session's specific files/lines/output with no generalizable lesson. If nothing qualifies, that is the correct outcome — do not invent content to pad a report.
3. WRITE (semantic memory, portable baseline). Append each accepted insight as one dated bullet to `~/.agent-flywheel/MEMORY.md` (create it with a one-line header if it doesn't exist yet: what, when it applies, why — one line each, newest at the bottom). This file has zero dependencies and always exists after the first run, regardless of which harness or project you're in.
4. WRITE (project-native, if present — additive, never a substitute for step 3). If this project ALSO has its own memory convention already in use — a memory/reasoning tool, an AGENTS.md or CLAUDE.md "Learned rules" section, a wiki page, a durable-memory MCP tool — record it there too, in that convention's format. Detect this from what's already in the repo/context; do not assume any specific tool exists.
5. PROCEDURALIZE (procedural memory). If the same multi-step workflow ran two or more times this session, or you can tell it will clearly recur, and no existing skill/doc/script already covers it, turn it into a short reusable skill, script, or checklist file rather than only prose memory. Skip this step if nothing repeatable came up.
6. DECAY. If something you just learned contradicts or obsoletes an existing bullet already in `~/.agent-flywheel/MEMORY.md` (or the project-native store), correct or remove the stale bullet in place rather than appending a second, contradicting one. Stale memory is worse than no memory.

Give a brief one-paragraph summary of exactly what was written (file paths + one-line gist), or state plainly that nothing qualified this session. Do not fabricate a report for a session that had nothing durable to record — "nothing new this session" is a valid and expected outcome, not a failure.
