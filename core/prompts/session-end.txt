This session just ended. Run the self-improvement reflection pass now, without asking for confirmation.

Ground yourself first: the session transcript is referenced below — read it directly (whatever read/file tool this harness exposes). This reflection pass's own conversation history starts blank; it is NOT pre-loaded with that transcript just because it resumed the session.

Prerequisite skills (optional, use when present, degrade to the built-in equivalent below when
absent — CHECK-EXISTING in step 5a surfaces these automatically; a match there means "use it", not
"build a duplicate"): `task-observer` (SCAN), `skill-creator` (CREATE), `skill-compactor` (COMPACT),
`learn-from-session` (GUARDRAILS), `level-up-coach` (LEVEL), `memorix`/`claude-mem` (MEMORY —
treat as the PRIMARY store, not just additive, whenever its tools are reachable in this session; it
is designed to work across every harness this project supports). None of these are required — every
stage below has a standalone fallback — but prefer the dedicated tool over the fallback whenever it
is installed and reachable, rather than running both. Feel free to use, extend, or create any skill
this stage needs; nothing here is meant to gate you away from a skill you already have.

This is the full pipeline (Reflexion/CoALA-grounded — episodic scan, semantic write, procedural write, decay — plus the skill-lifecycle and self-scoring stages below). Follow every stage in order; a stage with nothing to do is a valid, expected outcome — say so and move on, never pad it.

1. SCAN (episodic). Reread what happened this session: mistakes made and corrected, non-obvious facts discovered about this codebase/environment, explicit user preferences or corrections stated (especially anything marked as an interjection/override), repeatable procedures used more than once, and any friction (fighting the tool, guessing wrong, redoing work) a future session would hit again. If `task-observer` is installed, its own logging convention/location takes over this stage.

2. IDENTIFY. For each candidate from SCAN, classify it — this determines which later stage handles it:
   - Reusable fact/preference, but doesn't rise to "must change future behavior" → MEMORY (step 3).
   - The user explicitly corrected a specific behavior, or a mistake a prior guardrail already warned about recurred → GUARDRAILS (step 4).
   - The same multi-step workflow ran 3+ times (this session or per MEMORY.md history), or clearly will recur, and nothing already covers it → SKILL (step 5).
   Reject anything that's just a play-by-play of this session's specific files/lines/output with no generalizable lesson. If nothing qualifies anywhere, that is correct — do not invent content to pad a report.

3. MEMORY (semantic). If `memorix` (or `claude-mem`) tools are reachable in this session, use them as the PRIMARY store for every accepted MEMORY-class insight — call its store operation the same way you would for any user-requested memory. ALWAYS ALSO append the same insight as one dated bullet to `~/.agent-flywheel/MEMORY.md` (create it with a one-line header if it doesn't exist yet) regardless of whether memorix is present — this is the zero-dependency baseline that keeps the pipeline working on any machine, harness, or project even when memorix isn't configured there. If this project additionally has its own convention already in use (an AGENTS.md/CLAUDE.md "Learned rules" section, a wiki page), record it there too.

4. GUARDRAILS (binding corrections). For each GUARDRAILS-class item: if `learn-from-session` is installed, follow its own guardrail-file convention and location instead. Otherwise read `core/prompts/reference/guardrails-format.txt` (next to this file) and follow it exactly — numbered ledger at `~/.agent-flywheel/GUARDRAILS.md`, binding starting the next opportunity to apply it, not merely logged for later.

5. SKILL (procedural memory, lifecycle). For each SKILL-class item:
   a. CHECK-EXISTING — read `core/prompts/reference/skill-roots.txt` and follow its procedure (or run `agent-flywheel skill list`) before building anything. A match → UPDATE; nothing → CREATE.
   b. If `skill-creator` is installed, prefer it for CREATE. Otherwise read `core/prompts/reference/skill-authoring.txt` and follow the matching UPDATE or CREATE procedure (or run `agent-flywheel skill new`).
   c. COMPACT — mandatory immediately after step (b). If `skill-compactor` is installed, invoke it. Otherwise apply the COMPACT section of `skill-authoring.txt`. Never skip this because the skill "looks fine already."

6. LEVEL (self-score, this trigger only — skip entirely on a periodic/mid-session pass). If `level-up-coach` is installed, prefer its rubric/routing. Otherwise read `core/prompts/reference/level-rubric.txt` and follow it: score this session, append one line to `~/.agent-flywheel/LEVEL.md`.

7. DECAY. If anything learned this pass (in MEMORY, GUARDRAILS, or a skill) contradicts or obsoletes an existing entry, correct or remove the stale one in place rather than appending a second, contradicting entry anywhere — in memorix AND in MEMORY.md if both are in use. Stale memory is worse than no memory.

8. VISIBLE LOG. Append exactly one line to `~/.agent-flywheel/LEARN.log` in this format (create the file if absent, never overwrite prior lines):
   `<ISO8601 timestamp> [<trigger>] <one-line summary of what changed: files touched, G-number, skill name, or "nothing durable">`
   This is the human-visible ledger — the one place a user can `tail -f ~/.agent-flywheel/LEARN.log` or run `agent-flywheel log` and see what every past pass actually did, across every harness and project. Keep it to one line per pass; verbosity belongs in MEMORY.md, not here.

9. META (self-improvement of the loop itself — cadence-gated, configurable). Ask the gate whether the meta pass is due at THIS trigger point per the user's configured cadence (config.env: mode every-session|gap|days|off, trigger session-end|mid-session|both) — run `agent-flywheel self-improve --gate --trigger session-end --mark` (if `agent-flywheel` isn't on PATH in this pass, use `~/.agent-flywheel/bin/agent-flywheel` or `<flywheel-checkout>/bin/agent-flywheel`). If it exits 0 it prints the meta prompt — follow that procedure now: the loop reflecting on and improving ITSELF (its prompts, adapters, guardrail effectiveness, level trend, next-skill curriculum), reusing the file access this pass already has. If it exits non-zero, the meta pass is not due at this trigger — skip this step silently. This is what makes the improver improve itself on the user's chosen cadence with zero extra process.

Finish with a brief one-paragraph summary of exactly what was written across every stage above (file paths + one-line gist each), or state plainly that nothing qualified this session. Do not fabricate a report for a session that had nothing durable to record.
