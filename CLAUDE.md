# CLAUDE.md — agent-flywheel

See [AGENTS.md](AGENTS.md) — that file is the source of truth for working
on this repo (single-source-of-truth layout, load-bearing constraints,
conventions, verification commands). This file exists only because some
tooling looks for `CLAUDE.md` specifically; keep the two in sync rather
than letting either drift into its own copy of the same guidance. If they
ever disagree, `AGENTS.md` wins.
